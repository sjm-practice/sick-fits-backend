type User {
  id: ID! @unique
  name: String!
  email: String!
}

# createdAt, updatedAt (DateTime) are automatically included by prisma
type Item {
  id: ID! @unique
  title: String!
  description: String!
  image: String
  largeImage: String
  price: Int!
  # user: User!
}
